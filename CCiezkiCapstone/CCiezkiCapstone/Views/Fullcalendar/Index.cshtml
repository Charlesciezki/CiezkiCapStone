@model IEnumerable<CCiezkiCapstone.Models.FullcalendarModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <center>
    <h2>Fill out an appointment form</h2>
     </center>
            <p>
                <center>@Html.ActionLink("Request an appointment here", "Create")</center>
</p>
    <center>
    <table>

        <tr>
            @*<th>
                    @Html.DisplayNameFor(model => model.title)
                </th>*@
            <th>
                <center>@Html.DisplayNameFor(model => model.date)</center>
            </th>
        </tr>

        @foreach (var item in Model)
            {
            <tr>
                @*<td>
                        @Html.DisplayFor(modelItem => item.title)
                    </td>*@
                <td>
                    <center>@Html.DisplayFor(modelItem => item.date)</center>
                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id=item.event_id }) |*@
                    @*@Html.ActionLink("Details", "Details", new { id=item.event_id })*@
                    @*@Html.ActionLink("Delete", "Delete", new { id=item.event_id })*@
                </td>
            </tr>
        }

    </table>
</center>
